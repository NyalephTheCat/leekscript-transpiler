global a = 1; // test
var b;
if (a) {
    var c;
    var d = 1;
}
if (a) {
    stuff;
} else b;
for (var a: var b in c) {
    abc
}
for (var b in c) a;
while (a) {
    b;
}
do 1 while (a)

/*
    All The stuff to fix:

// Probably the arg1, arg2 part to fix
function name(arg1, arg2) {
    return something
}

// Throw depreciation error, maybe while parsing ? Maybe after
a === b

/// Also check for consise body to not start with "{"
*/